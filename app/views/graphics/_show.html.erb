<% if authorized? %>
	<%= link_to( image_tag("fail.png", :size => "16x16"), { :controller => 'graphics', :action => 'destroy', :id => @graphic, :page_section_id => @page_section}, {:confirm => 'Are you sure?', :method => :delete, :title => "Delete"}) %>
	<%= link_to( 'delete graphic', { :controller => 'graphics', :action => 'destroy', :id => @graphic, :page_section_id => @page_section}, {:confirm => 'Are you sure?', :method => :delete, :title => "Delete"}) %>
	<br />
<% end %>
<%= image_tag @graphic.public_filename, :size => "#{@graphic.width}x#{@graphic.height}", :alt => @graphic.alt_text, :class => @graphic.class_name %>